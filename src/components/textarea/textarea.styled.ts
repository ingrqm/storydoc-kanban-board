import styled from 'styled-components';

export const Textarea = styled.textarea`
  border: none;
  background: none;
  padding: 0;
  width: 100%;
  font-size: inherit;
  line-height: inherit;
  resize: none;
`;
